mixin field(colSize, name, text, value, placeholder, autofocus, id)
  div(class='small-#{colSize} columns' id='#{id}')
    label(for=name) #{text}:
    input(value=value, type='text', name=name, placeholder=placeholder, autofocus=autofocus)
