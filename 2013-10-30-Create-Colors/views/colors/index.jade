extends ../template/layout

block content
  .row
    .small-12.columns
      h1 Colors Page
  .row
    .small-3.columns
      a.button.small.radius(href="/") Home
    .small-9.columns
      table#colors
        thead: tr
          each color in colors
            th=color
        tbody
          tbody: tr
            each color in colors
              td.color(style="background-color:#{color};")
